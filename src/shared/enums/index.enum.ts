export enum FILTEROPERATORS {
  OR = 'or',
  AND = 'and',
}

export enum USER_MODELS {
  PEOPLE = 'people',
  SPONSOR = 'sponsor',
}

export enum PeopleStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  NEW = 'new',
}

export enum EMAIL_TYPES_ENUM {
  ORDER_REQUEST = 'ORDER_REQUEST',
  FORGOT_PASSWORD = 'FORGOT_PASSWORD',
  ADD_USER = 'ADD_USER',
  VERIFY = 'VERIFY',
}
